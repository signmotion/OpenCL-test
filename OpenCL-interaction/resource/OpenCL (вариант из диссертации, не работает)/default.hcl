// Настройки мира

#if 0
- Передаются в качестве параметров define

// Размер мира
const size_t N = 512;
const size_t M = 512 / 2;
// 1 метр содержит 5 неделимых ячеек (по 20 см)
const float SCALE = 1.0f / 5.0f;

// Максимально возможное количество материи мира
const size_t MAX_COUNT_MATTER = 256;

#endif
